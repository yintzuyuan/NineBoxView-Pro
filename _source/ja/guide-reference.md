# {{GUIDE_NAV_REFERENCE}}

{{GUIDE_NAV_REFERENCE}}機能では、一組の文字を入力して9マスの周囲に表示し、現在編集中の文字が異なるコンテキストでどのような視覚効果を持つかを観察できます。

## 目次

- [{{REFERENCE_PANEL_TITLE|}}を開く](#参照文字パネルを開く)
- [入力形式](#入力形式)
- [操作方法](#操作方法)
- [コンテキストメニュー](#コンテキストメニュー)
- [ドラッグ＆ドロップ](#ドラッグドロップ)

## {{REFERENCE_PANEL_TITLE|}}を開く

{{REFERENCE_PANEL_TITLE|}}を開く方法は2つあります：

1. **ツールバーボタン**：ツールバーの {{ICON_REFERENCE}} ボタンをクリック
2. **コンテキストメニュー**：9マス内で右クリックし、「{{CONTEXT_MENU_SHOW_REFERENCE_INPUT|}}」を選択

パネルが開くとテキスト入力エリアが表示され、ここで参照文字を入力できます。

## 入力形式

{{REFERENCE_PANEL_TITLE|}}は複数の入力形式をサポートしています：

### 文字を直接入力

文字を直接入力すると、すべての文字が1文字ずつ解析されます：

```
あいうえおか
```

glyph name を指定する場合は、`/` で始めてください（以下を参照）。

### Glyph Name形式

スラッシュ `/` で始めて文字名を指定します：

```
/adieresis /Agrave /uni6C38
```

### Unicode形式

複数のUnicode入力方法をサポートしています：

| 形式 | 例 | 説明 |
|-----|------|------|
| `/uniXXXX` | `/uni6C38` | 4桁16進数（公式形式） |
| `/uXXXXX` | `/u06C38` | 5桁16進数 |
| `/XXXX` | `/6C38` | 4-5桁の数字のみ（簡易形式） |

### 混合入力

異なる形式を混合して使用できます：

```
あ /adieresis ア /uni0041 い
```

## 操作方法

### 参照文字を保存

入力完了後、「{{SAVE_REFERENCE_BUTTON|}}」ボタンをクリックして{{PRESETS_WINDOW_TITLE|}}に保存します。

### ランダム配置

9マス内をクリック（修飾キーなし）すると、参照文字が周囲の位置にランダムに再配置されます。

> [!NOTE]
> 中央セル（位置 4）は常に現在編集中の文字を表示し、参照文字に置き換えられることはありません。

## コンテキストメニュー

{{REFERENCE_PANEL_TITLE|}}の入力エリアで右クリックするとコンテキストメニューが開きます：

| 項目 | 説明 |
|-----|------|
| {{CONTEXT_MENU_GLYPH_PICKER|}} | グリフピッカーを開き、複数の文字を選択して挿入 |
| {{CONTEXT_MENU_INSERT_AT_CURSOR|}} | 参照文字をGlyphs編集ビューのカーソル位置に挿入 |
| {{CONTEXT_MENU_OPEN_IN_NEW_TAB|}} | 参照文字を新しいタブで開く |

## ドラッグ＆ドロップ

### 基本的なドラッグ

9マス内の文字を他の位置にドラッグ：

| 操作 | 効果 |
|-----|------|
| 通常のドラッグ | 位置を交換 |
| Option + ドラッグ | 参照文字をコピー（元は保持） |

### Glyphs編集ビューへドラッグ

9マス内の文字をGlyphsの編集ビューにドラッグ：

| 操作 | 効果 |
|-----|------|
| 通常のドラッグ | カーソル位置に文字を挿入 |
| Option + ドラッグ | その文字を新しいタブで開く |

### {{REFERENCE_PANEL_TITLE|}}へドラッグ

9マス内の文字を入力エリアにドラッグして、参照文字を素早く追加：

- 9マス内のどの位置の文字も受け付け
- 文字はカーソル位置に挿入
- 前後に自動的にスペースを追加

> [!TIP]
> その他のドラッグ操作（Cmdモード、Shiftモードなど）は[高度な機能](advanced.html#ドラッグ高度な操作)を参照してください。

## 使用のヒント

### クイックテストグループ

1. よく使うテスト文字を入力（例：「あアいイ」）
2. 「{{SAVE_REFERENCE_BUTTON|}}」をクリックして{{PRESETS_WINDOW_TITLE|}}に保存
3. 後で{{PRESETS_WINDOW_TITLE|}}から素早くロード

### 間隔を確認

同じ文字の配置効果を観察したい場合は、単一の文字を入力するだけです：

```
あア
```

システムが9マスの各位置にその文字を自動的に繰り返し表示し、字間と視覚バランスを観察しやすくなります。

## 関連機能

- [ロック文字機能](guide-lock)：特定位置の文字を固定
- [{{PRESETS_WINDOW_TITLE|}}機能](guide-presets)：よく使う文字セットを保存
- [ホームに戻る](guide)
