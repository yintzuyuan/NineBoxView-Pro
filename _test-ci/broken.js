// CI Test - JavaScript with undefined variables

// 故意的錯誤：使用未定義的變數
console.log(undefinedVariable);

// 故意的錯誤：呼叫不存在的函數
nonExistentFunction();

// 正常的程式碼
const validVar = "This is valid";
console.log(validVar);
